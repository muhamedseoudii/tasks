  class ChatModel {
    late String userImage, userName, message, time;
    late bool isread;

    ChatModel(
        {required this.userImage,
        required this.userName,
        required this.message,
        required this.time,
        required this.isread});
  }
